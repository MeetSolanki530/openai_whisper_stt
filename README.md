# Streamlit Whisper STT

A simple speech-to-text (STT) app using **OpenAI Whisper** and **Streamlit**. 
## Installation

```bash
python -m venv venv
source venv/bin/activate   # Linux/Mac
venv\Scripts\activate      # Windows
pip install -r requirements.txt
